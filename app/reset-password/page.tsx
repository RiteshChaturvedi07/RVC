'use client'
import { useEffect, useMemo, useState } from 'react'
import { useRouter } from 'next/navigation'
import { CheckCircle2, Eye, EyeOff, KeyRound, Lock } from 'lucide-react'
import { createClient } from '@/lib/supabase/client'
export default function ResetPasswordPage(){const db=createClient(),router=useRouter(),[password,setPassword]=useState(''),[confirm,setConfirm]=useState(''),[show,setShow]=useState(false),[loading,setLoading]=useState(false),[error,setError]=useState(''),[success,setSuccess]=useState(false),[ready,setReady]=useState(false);useEffect(()=>{const init=async()=>{const{data:{session}}=await db.auth.getSession();if(!session)setError('This recovery link is invalid or has expired. Request a new one.');setReady(true)};void init()},[]);const score=useMemo(()=>[password.length>=8,/[A-Z]/.test(password),/[0-9]/.test(password),/[^A-Za-z0-9]/.test(password)].filter(Boolean).length,[password]),label=score<=1?'Weak':score<=3?'Medium':'Strong',color=score<=1?'bg-red-500':score<=3?'bg-amber-500':'bg-emerald-500';const update=async(e:React.FormEvent)=>{e.preventDefault();if(password.length<8)return setError('Use at least 8 characters.');if(password!==confirm)return setError('Passwords do not match.');setLoading(true);setError('');const{error}=await db.auth.updateUser({password});setLoading(false);if(error)return setError(error.message);setSuccess(true);setTimeout(()=>router.replace('/login'),2000)};return <main className="grid min-h-screen place-items-center bg-gradient-to-br from-slate-50 via-indigo-50 to-violet-100 p-4 dark:from-slate-950 dark:via-slate-900 dark:to-indigo-950"><section className="w-full max-w-md rounded-3xl border border-white/60 bg-white/80 p-8 shadow-2xl backdrop-blur-xl dark:border-slate-800 dark:bg-slate-900/80"><span className="grid size-14 place-items-center rounded-2xl bg-primary text-primary-foreground"><KeyRound/></span><h1 className="mt-5 text-2xl font-bold">Set new password</h1><p className="mt-2 text-sm text-muted-foreground">Please enter and confirm your new password below.</p>{success?<p className="mt-6 rounded-xl bg-emerald-500/10 p-4 text-emerald-700"><CheckCircle2 className="mr-1 inline size-4"/>Password updated successfully. Redirecting to login…</p>:<form onSubmit={update} className="mt-6 space-y-3"><label className="relative block"><Lock className="absolute left-3 top-3.5 size-4 text-muted-foreground"/><input disabled={!ready} type={show?'text':'password'} value={password} onChange={e=>setPassword(e.target.value)} placeholder="New password" className="h-11 w-full rounded-xl border bg-background pl-10 pr-11"/><button type="button" onClick={()=>setShow(!show)} className="absolute right-3 top-3">{show?<EyeOff size={18}/>:<Eye size={18}/>}</button></label><input disabled={!ready} type={show?'text':'password'} value={confirm} onChange={e=>setConfirm(e.target.value)} placeholder="Confirm new password" className="h-11 w-full rounded-xl border bg-background px-3"/><div><div className="h-2 overflow-hidden rounded bg-muted"><i className={`block h-full ${color}`} style={{width:`${score*25}%`}}/></div><small className="mt-1 block text-muted-foreground">Password strength: {label}</small></div>{error&&<p className="text-sm text-red-600">{error}</p>}<button disabled={loading||!ready} className="h-11 w-full rounded-xl bg-primary font-semibold text-primary-foreground disabled:opacity-60">{loading?'Updating password…':'Update password'}</button></form>}</section></main>}
