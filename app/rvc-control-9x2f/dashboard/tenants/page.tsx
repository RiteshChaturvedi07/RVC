'use client'

import { useMemo, useState } from 'react'
import { AnimatePresence, motion } from 'framer-motion'
import { Building2, Check, Eye, Filter, MoreHorizontal, Plus, Search, ShieldAlert, UserRound, X } from 'lucide-react'

const seed = [
  { id: 'RVC-1024', name: 'Spice Route', owner: 'Riya Mehta', vertical: 'Restaurant ERP', city: 'Indore', plan: 'Growth', status: 'Active', mrr: 'INR 2,999', joined: '12 Jun 2025' },
  { id: 'RVC-1023', name: 'Bright Future Academy', owner: 'Amit Shah', vertical: 'School ERP', city: 'Jaipur', plan: 'Pro', status: 'Active', mrr: 'INR 9,999', joined: '10 Jun 2025' },
  { id: 'RVC-1022', name: 'MediCare Plus', owner: 'Dr. Neha Rao', vertical: 'Hospital HIS', city: 'Lucknow', plan: 'Growth', status: 'Trial', mrr: 'INR 0', joined: '08 Jun 2025' },
  { id: 'RVC-1021', name: 'Pulse Fitness', owner: 'Karan Sethi', vertical: 'Gym Management', city: 'Nagpur', plan: 'Starter', status: 'At risk', mrr: 'INR 999', joined: '05 Jun 2025' },
  { id: 'RVC-1020', name: 'The Daily Basket', owner: 'Simran Kaur', vertical: 'Retail POS', city: 'Coimbatore', plan: 'Growth', status: 'Active', mrr: 'INR 2,999', joined: '03 Jun 2025' },
]

type Tenant = typeof seed[number]
const statusStyles: Record<string, string> = { Active: 'bg-emerald-500/10 text-emerald-500', Trial: 'bg-amber-500/15 text-amber-600', 'At risk': 'bg-red-500/10 text-red-500' }

export default function TenantsPage() {
  const [query, setQuery] = useState('')
  const [filter, setFilter] = useState('All')
  const [tenants, setTenants] = useState(seed)
  const [selected, setSelected] = useState<Tenant | null>(null)
  const [addOpen, setAddOpen] = useState(false)
  const [notice, setNotice] = useState('')
  const filtered = useMemo(() => tenants.filter((tenant) => (filter === 'All' || tenant.status === filter) && `${tenant.name} ${tenant.owner} ${tenant.city} ${tenant.id}`.toLowerCase().includes(query.toLowerCase())), [tenants, query, filter])
  const toggleStatus = (tenant: Tenant) => { setTenants((current) => current.map((item) => item.id === tenant.id ? { ...item, status: item.status === 'Active' ? 'At risk' : 'Active' } : item)); setNotice(`${tenant.name} status updated and logged to the audit trail.`); setTimeout(() => setNotice(''), 2600) }

  return <div className="space-y-8"><div className="flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div><p className="text-sm font-medium text-primary">Workspace / Tenants</p><h1 className="mt-1 text-3xl font-bold tracking-tight sm:text-4xl">Tenant management</h1><p className="mt-2 text-sm text-muted-foreground">Provision, monitor, and protect every business on RVC.</p></div><button type="button" onClick={() => setAddOpen(true)} className="inline-flex items-center justify-center gap-2 rounded-xl bg-primary px-4 py-3 text-sm font-semibold text-primary-foreground shadow-lg shadow-primary/20"><Plus className="size-4" /> Add tenant</button></div>
    {notice && <motion.div initial={{ opacity: 0, y: -8 }} animate={{ opacity: 1, y: 0 }} className="flex items-center gap-2 rounded-xl border border-emerald-500/20 bg-emerald-500/10 px-4 py-3 text-sm text-emerald-600"><Check className="size-4" />{notice}</motion.div>}
    <div className="grid gap-4 sm:grid-cols-3"><Summary label="Total tenants" value={tenants.length.toString()} /><Summary label="Active MRR" value="INR 16,996" /><Summary label="Needs review" value="2" /></div>
    <section className="overflow-hidden rounded-2xl border border-border bg-card"><div className="flex flex-col gap-3 border-b border-border p-4 sm:flex-row"><div className="relative flex-1"><Search className="absolute left-3 top-1/2 size-4 -translate-y-1/2 text-muted-foreground" /><input value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Search tenants, owners, cities..." className="h-11 w-full rounded-xl border border-border bg-background pl-10 pr-4 text-sm outline-none focus:ring-2 focus:ring-primary/30" /></div><div className="flex items-center gap-2"><Filter className="size-4 text-muted-foreground" /><select value={filter} onChange={(event) => setFilter(event.target.value)} className="h-11 rounded-xl border border-border bg-background px-3 text-sm outline-none"><option>All</option><option>Active</option><option>Trial</option><option>At risk</option></select></div></div><div className="overflow-x-auto"><table className="w-full min-w-[850px] text-left text-sm"><thead className="bg-muted/40 text-xs uppercase tracking-wider text-muted-foreground"><tr><th className="px-5 py-4 font-semibold">Business</th><th className="px-5 py-4 font-semibold">Vertical</th><th className="px-5 py-4 font-semibold">Plan</th><th className="px-5 py-4 font-semibold">MRR</th><th className="px-5 py-4 font-semibold">Status</th><th className="px-5 py-4 text-right font-semibold">Actions</th></tr></thead><tbody>{filtered.map((tenant) => <tr key={tenant.id} className="border-t border-border/70 transition-colors hover:bg-muted/25"><td className="px-5 py-4"><button type="button" onClick={() => setSelected(tenant)} className="text-left"><p className="font-semibold hover:text-primary">{tenant.name}</p><p className="mt-1 text-xs text-muted-foreground">{tenant.owner} · {tenant.city} · {tenant.id}</p></button></td><td className="px-5 py-4 text-muted-foreground">{tenant.vertical}</td><td className="px-5 py-4"><span className="rounded-lg bg-primary/10 px-2.5 py-1 text-xs font-semibold text-primary">{tenant.plan}</span></td><td className="px-5 py-4 font-semibold">{tenant.mrr}</td><td className="px-5 py-4"><span className={`rounded-full px-2.5 py-1 text-xs font-semibold ${statusStyles[tenant.status]}`}>{tenant.status}</span></td><td className="px-5 py-4"><div className="flex justify-end gap-1"><button type="button" aria-label={`View ${tenant.name}`} onClick={() => setSelected(tenant)} className="rounded-lg p-2 text-muted-foreground hover:bg-muted hover:text-foreground"><Eye className="size-4" /></button><button type="button" aria-label={`Toggle ${tenant.name}`} onClick={() => toggleStatus(tenant)} className="rounded-lg p-2 text-muted-foreground hover:bg-muted hover:text-foreground"><ShieldAlert className="size-4" /></button><button type="button" className="rounded-lg p-2 text-muted-foreground hover:bg-muted hover:text-foreground"><MoreHorizontal className="size-4" /></button></div></td></tr>)}</tbody></table></div><div className="border-t border-border px-5 py-4 text-xs text-muted-foreground">Showing {filtered.length} of {tenants.length} tenants · All actions are audit logged</div></section>
    <AnimatePresence>{selected && <TenantDrawer tenant={selected} onClose={() => setSelected(null)} onToggle={() => { toggleStatus(selected); setSelected(null) }} />}</AnimatePresence><AnimatePresence>{addOpen && <AddTenant onClose={() => setAddOpen(false)} onCreate={(name) => { setNotice(`${name} added to onboarding queue.`); setAddOpen(false); setTimeout(() => setNotice(''), 2600) }} />}</AnimatePresence>
  </div>
}
function Summary({ label, value }: { label: string; value: string }) { return <div className="rounded-2xl border border-border bg-card p-5"><p className="text-sm text-muted-foreground">{label}</p><p className="mt-2 text-2xl font-bold">{value}</p></div> }
function TenantDrawer({ tenant, onClose, onToggle }: { tenant: Tenant; onClose: () => void; onToggle: () => void }) { return <motion.div className="fixed inset-0 z-[55] flex justify-end bg-slate-950/50 backdrop-blur-sm" initial={{ opacity: 0 }} animate={{ opacity: 1 }} exit={{ opacity: 0 }} onClick={onClose}><motion.aside className="h-full w-full max-w-lg overflow-y-auto border-l border-border bg-card p-6 shadow-2xl" initial={{ x: '100%' }} animate={{ x: 0 }} exit={{ x: '100%' }} transition={{ type: 'spring', damping: 28 }} onClick={(event) => event.stopPropagation()}><div className="flex items-start justify-between"><div><p className="text-xs font-semibold uppercase tracking-[0.2em] text-primary">Tenant profile</p><h2 className="mt-2 text-2xl font-bold">{tenant.name}</h2><p className="mt-1 text-sm text-muted-foreground">{tenant.id} · {tenant.vertical}</p></div><button type="button" onClick={onClose} className="rounded-xl p-2 text-muted-foreground hover:bg-muted"><X className="size-5" /></button></div><div className="mt-8 grid gap-3 sm:grid-cols-2"><Info label="Owner" value={tenant.owner} icon={<UserRound className="size-4" />} /><Info label="Location" value={tenant.city} icon={<Building2 className="size-4" />} /><Info label="Plan" value={tenant.plan} icon={<CreditCardIcon />} /><Info label="Joined" value={tenant.joined} icon={<ClockIcon />} /></div><div className="mt-8 rounded-2xl bg-muted/50 p-5"><p className="text-sm font-semibold">Account actions</p><p className="mt-1 text-xs leading-5 text-muted-foreground">Actions are simulated for this UI pass and should be protected by server-side permissions in production.</p><div className="mt-4 grid gap-2 sm:grid-cols-2"><button type="button" onClick={onToggle} className="rounded-xl border border-border px-3 py-2.5 text-sm font-semibold hover:bg-background">{tenant.status === 'Active' ? 'Suspend access' : 'Reactivate access'}</button><button type="button" onClick={() => alert('Impersonation request logged.')} className="rounded-xl bg-primary px-3 py-2.5 text-sm font-semibold text-primary-foreground">Impersonate tenant</button></div></div><div className="mt-6 rounded-2xl border border-border p-5"><p className="text-sm font-semibold">Recent audit events</p>{['Admin viewed tenant details', 'Plan entitlement checked', 'Usage snapshot refreshed'].map((event, index) => <div key={event} className="flex items-center justify-between border-b border-border/70 py-3 text-sm last:border-0"><span>{event}</span><span className="text-xs text-muted-foreground">{index + 2}h ago</span></div>)}</div></motion.aside></motion.div> }
function Info({ label, value, icon }: { label: string; value: string; icon: React.ReactNode }) { return <div className="rounded-xl border border-border bg-background p-4"><div className="flex items-center gap-2 text-muted-foreground">{icon}<span className="text-xs">{label}</span></div><p className="mt-2 font-semibold">{value}</p></div> }
function AddTenant({ onClose, onCreate }: { onClose: () => void; onCreate: (name: string) => void }) { const [name, setName] = useState(''); return <motion.div className="fixed inset-0 z-[55] grid place-items-center bg-slate-950/50 p-4 backdrop-blur-sm" initial={{ opacity: 0 }} animate={{ opacity: 1 }} exit={{ opacity: 0 }} onClick={onClose}><motion.div className="w-full max-w-md rounded-2xl border border-border bg-card p-6 shadow-2xl" initial={{ opacity: 0, y: 12 }} animate={{ opacity: 1, y: 0 }} onClick={(event) => event.stopPropagation()}><div className="flex items-start justify-between"><div><p className="text-xs font-semibold uppercase tracking-[0.2em] text-primary">Provision tenant</p><h2 className="mt-2 text-2xl font-bold">Add a new business</h2></div><button type="button" onClick={onClose} className="rounded-xl p-2 text-muted-foreground hover:bg-muted"><X className="size-5" /></button></div><label className="mt-6 block text-sm font-semibold">Business name<input value={name} onChange={(event) => setName(event.target.value)} className="mt-2 h-11 w-full rounded-xl border border-border bg-background px-3 text-sm outline-none focus:ring-2 focus:ring-primary/30" placeholder="e.g. North Star Academy" /></label><button type="button" disabled={!name.trim()} onClick={() => onCreate(name.trim())} className="mt-6 w-full rounded-xl bg-primary px-4 py-3 text-sm font-semibold text-primary-foreground disabled:cursor-not-allowed disabled:opacity-50">Create onboarding invite</button></motion.div></motion.div> }
function CreditCardIcon() { return <span className="text-primary">INR </span> }
function ClockIcon() { return <span className="text-primary">◷</span> }
