'use client'

import { useState } from 'react'
import { motion } from 'framer-motion'
import { ArrowRight, Check, ChefHat, CircleAlert, Clock3, IndianRupee, MoreHorizontal, PackageCheck, Plus, Table2, Users } from 'lucide-react'
import { orders, RevenueChart, RestaurantStatCard, statusClasses, TopItemsChart } from '@/components/restaurant/restaurant-widgets'

export default function RestaurantOverview() {
  const [range, setRange] = useState('Week')
  const [activeOrders, setActiveOrders] = useState(orders)
  const advance = (id: string) => setActiveOrders((current) => current.map((order) => order.id === id ? { ...order, status: order.status === 'New' ? 'Preparing' : order.status === 'Preparing' ? 'Ready' : 'Served', fresh: false } : order))
  return <div className="space-y-6">
    <div className="flex flex-col justify-between gap-4 md:flex-row md:items-end"><div><p className="text-sm font-medium text-primary">Saturday, 18 October 2025</p><h2 className="mt-1 text-3xl font-semibold tracking-tight">Good morning, Rajesh</h2><p className="mt-1 text-muted-foreground">Here is what is happening at Spice Kitchen today.</p></div><button className="inline-flex items-center justify-center gap-2 rounded-xl bg-primary px-4 py-2.5 text-sm font-medium text-primary-foreground shadow-sm hover:opacity-90"><Plus size={17} /> New order</button></div>
    <div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4"><RestaurantStatCard title="Today's Revenue" value="INR 42,680" change="12.8%" icon={IndianRupee} /><RestaurantStatCard title="Orders Today" value="128" change="8.4%" icon={PackageCheck} /><RestaurantStatCard title="Active Tables" value="14 / 24" change="3 tables" icon={Table2} /><RestaurantStatCard title="Avg Order Value" value="INR 684" change="5.6%" icon={Clock3} /></div>
    <div className="grid gap-6 xl:grid-cols-[1.5fr_1fr]"><section className="rounded-2xl border border-border bg-card p-5 shadow-sm"><div className="mb-2 flex flex-col gap-3 sm:flex-row sm:items-center sm:justify-between"><div><h3 className="font-semibold">Revenue overview</h3><p className="text-sm text-muted-foreground">Track your sales performance</p></div><div className="flex rounded-lg bg-secondary p-1">{['Day', 'Week', 'Month'].map((item) => <button key={item} onClick={() => setRange(item)} className={`rounded-md px-3 py-1.5 text-xs font-medium ${range === item ? 'bg-card text-foreground shadow-sm' : 'text-muted-foreground'}`}>{item}</button>)}</div></div><RevenueChart /></section><section className="rounded-2xl border border-border bg-card p-5 shadow-sm"><div className="mb-3 flex items-center justify-between"><div><h3 className="font-semibold">Top-selling this week</h3><p className="text-sm text-muted-foreground">By number of orders</p></div><button className="text-sm text-primary">View menu</button></div><TopItemsChart /></section></div>
    <div className="grid gap-6 xl:grid-cols-[1.35fr_1fr]"><section className="rounded-2xl border border-border bg-card p-5 shadow-sm"><div className="mb-4 flex items-center justify-between"><div><h3 className="font-semibold">Live order status</h3><p className="text-sm text-muted-foreground">Updates appear here as orders arrive</p></div><span className="flex items-center gap-1.5 text-xs text-emerald-600"><span className="size-2 animate-pulse rounded-full bg-emerald-500" /> Live</span></div><div className="space-y-3">{activeOrders.map((order) => <motion.div layout key={order.id} className={`flex flex-col gap-3 rounded-xl border border-border p-4 sm:flex-row sm:items-center sm:justify-between ${order.fresh ? 'bg-primary/5 ring-1 ring-primary/20' : 'bg-background'}`}><div className="flex items-center gap-3"><div className={`grid size-10 place-items-center rounded-xl ${order.fresh ? 'bg-primary text-primary-foreground' : 'bg-secondary text-muted-foreground'}`}><ChefHat size={18} /></div><div><div className="flex items-center gap-2"><p className="font-medium">{order.id}</p><span className="text-xs text-muted-foreground">{order.table}</span>{order.fresh && <span className="rounded-full bg-accent px-2 py-0.5 text-[10px] font-semibold text-accent-foreground">NEW</span>}</div><p className="text-sm text-muted-foreground">{order.items}</p></div></div><div className="flex items-center justify-between gap-3 sm:justify-end"><div className="text-right"><p className="font-semibold">INR {order.total}</p><p className="text-xs text-muted-foreground">{order.time}</p></div><span className={`rounded-full px-2.5 py-1 text-xs font-medium ${statusClasses[order.status]}`}>{order.status}</span>{order.status !== 'Served' && <button onClick={() => advance(order.id)} className="rounded-lg border border-border p-2 hover:bg-secondary" aria-label={`Advance ${order.id}`}><ArrowRight size={15} /></button>}</div></motion.div>)}</div></section><section className="rounded-2xl border border-border bg-card p-5 shadow-sm"><div className="mb-4 flex items-center justify-between"><div><h3 className="font-semibold">Today at a glance</h3><p className="text-sm text-muted-foreground">Operations snapshot</p></div><MoreHorizontal size={18} className="text-muted-foreground" /></div><div className="space-y-4"><div className="flex items-center justify-between rounded-xl bg-secondary/70 p-4"><div className="flex items-center gap-3"><div className="grid size-9 place-items-center rounded-lg bg-emerald-500/15 text-emerald-600"><Check size={17} /></div><div><p className="text-sm font-medium">Kitchen on track</p><p className="text-xs text-muted-foreground">Average prep time 14 min</p></div></div><span className="text-sm font-semibold">92%</span></div><div className="flex items-center justify-between rounded-xl bg-secondary/70 p-4"><div className="flex items-center gap-3"><div className="grid size-9 place-items-center rounded-lg bg-amber-500/15 text-amber-600"><CircleAlert size={17} /></div><div><p className="text-sm font-medium">Low-stock alerts</p><p className="text-xs text-muted-foreground">3 ingredients need attention</p></div></div><button className="text-sm font-medium text-primary">Review</button></div><div className="flex items-center justify-between rounded-xl bg-secondary/70 p-4"><div className="flex items-center gap-3"><div className="grid size-9 place-items-center rounded-lg bg-indigo-500/15 text-primary"><Users size={17} /></div><div><p className="text-sm font-medium">Returning customers</p><p className="text-xs text-muted-foreground">This week</p></div></div><span className="text-sm font-semibold">38%</span></div></div></section></div>
  </div>
}
