'use client'
import { useState } from 'react'
import Link from 'next/link'
import { ArrowLeft, CheckCircle2, KeyRound, Mail } from 'lucide-react'
import { createClient } from '@/lib/supabase/client'
export default function ForgotPasswordPage(){const db=createClient(),[email,setEmail]=useState(''),[sent,setSent]=useState(false),[loading,setLoading]=useState(false),[error,setError]=useState('');const send=async(e:React.FormEvent)=>{e.preventDefault();if(!/^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(email))return setError('Enter a valid registered email address.');setLoading(true);setError('');const{error}=await db.auth.resetPasswordForEmail(email,{redirectTo:`${window.location.origin}/reset-password`});setLoading(false);if(error)setError(error.message);else setSent(true)};return <main className="grid min-h-screen place-items-center bg-gradient-to-br from-slate-50 via-indigo-50 to-violet-100 p-4 dark:from-slate-950 dark:via-slate-900 dark:to-indigo-950"><section className="w-full max-w-md rounded-3xl border border-white/60 bg-white/80 p-8 shadow-2xl backdrop-blur-xl dark:border-slate-800 dark:bg-slate-900/80">{sent?<div className="text-center"><span className="mx-auto grid size-14 place-items-center rounded-2xl bg-emerald-500/10 text-emerald-600"><CheckCircle2/></span><h1 className="mt-5 text-2xl font-bold">Check your inbox</h1><p className="mt-3 text-sm text-muted-foreground">We sent a secure password-reset link to <b>{email}</b>.</p><button onClick={()=>setSent(false)} className="mt-5 text-sm font-semibold text-primary">Resend email</button></div>:<><span className="grid size-14 place-items-center rounded-2xl bg-primary text-primary-foreground"><KeyRound/></span><h1 className="mt-5 text-2xl font-bold">Reset your password</h1><p className="mt-2 text-sm text-muted-foreground">Enter your registered email address to receive a secure recovery link.</p><form onSubmit={send} className="mt-6"><label className="relative block"><Mail className="absolute left-3 top-3.5 size-4 text-muted-foreground"/><input autoFocus type="email" value={email} onChange={e=>setEmail(e.target.value)} placeholder="you@example.com" className="h-11 w-full rounded-xl border bg-background pl-10 pr-3 outline-none"/></label>{error&&<p className="mt-2 text-sm text-red-600">{error}</p>}<button disabled={loading} className="mt-4 h-11 w-full rounded-xl bg-primary font-semibold text-primary-foreground disabled:opacity-60">{loading?'Sending reset link…':'Send reset link'}</button></form></>}<Link href="/login" className="mt-7 flex items-center justify-center gap-1 text-sm text-muted-foreground hover:text-foreground"><ArrowLeft size={15}/>Back to login</Link></section></main>}
