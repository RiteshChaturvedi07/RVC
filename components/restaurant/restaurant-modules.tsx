'use client'
import { useEffect, useState } from 'react'
import { Download, Plus, Save } from 'lucide-react'
import { createClient } from '@/lib/supabase/client'
import { currentRestaurantTenant } from '@/lib/restaurant'
import { RestaurantSimpleWorkspace } from './restaurant-workspaces'
function useRestaurant<T>(query:(tenant:string)=>Promise<T>){const[data,setData]=useState<T|null>(null);const[tenant,setTenant]=useState('');const[error,setError]=useState('');const reload=async()=>{try{const t=tenant||await currentRestaurantTenant();setTenant(t);setData(await query(t))}catch(e){setError(e instanceof Error?e.message:'Unable to load')}};useEffect(()=>{reload()},[]);return{data,tenant,error,reload}}
const s=()=>createClient(); const money=(x:number)=>`₹${Number(x).toLocaleString('en-IN')}`
export function RestaurantCustomers(){const state=useRestaurant(async t=>{const{data,error}=await s().from('restaurant_orders').select('customer_phone,total,created_at').eq('tenant_id',t).not('customer_phone','is',null);if(error)throw error;const map=new Map<string,{phone:string;orders:number;spent:number;last:string}>();(data??[]).forEach((o:any)=>{const a=map.get(o.customer_phone)||{phone:o.customer_phone,orders:0,spent:0,last:o.created_at};a.orders++;a.spent+=Number(o.total);if(o.created_at>a.last)a.last=o.created_at;map.set(o.customer_phone,a)});return[...map.values()]});if(!state.data)return <RestaurantSimpleWorkspace title="Customers" description={state.error||'Loading real guest order history…'}/>;return <RestaurantSimpleWorkspace title="Customers" description="Guest records are built automatically from QR orders."><div className="space-y-2">{state.data.map(c=><div key={c.phone} className="flex justify-between rounded-xl border border-border bg-card p-4"><div><b>{c.phone}</b><p className="text-sm text-muted-foreground">Last order {new Date(c.last).toLocaleDateString('en-IN')}</p></div><div className="text-right"><b>{money(c.spent)}</b><p className="text-sm text-muted-foreground">{c.orders} orders</p></div></div>)}{!state.data.length&&<p className="text-muted-foreground">Customers appear after guests provide a phone number while ordering.</p>}</div></RestaurantSimpleWorkspace>}
export function RestaurantFinance(){const state=useRestaurant(async t=>{const{data,error}=await s().from('restaurant_orders').select('total,payment_status,payment_method,created_at').eq('tenant_id',t);if(error)throw error;return data??[]});if(!state.data)return <RestaurantSimpleWorkspace title="Finance" description={state.error||'Loading live payment records…'}/>;const records=state.data;const paid=records.filter((o:any)=>o.payment_status==='paid');const cash=paid.filter((o:any)=>o.payment_method==='cash').reduce((x:number,o:any)=>x+Number(o.total),0);const online=paid.filter((o:any)=>o.payment_method==='online').reduce((x:number,o:any)=>x+Number(o.total),0);const exportCsv=()=>{const csv='payment_status,payment_method,total,created_at\n'+records.map((o:any)=>`${o.payment_status},${o.payment_method??''},${o.total},${o.created_at}`).join('\n');const a=document.createElement('a');a.href=URL.createObjectURL(new Blob([csv],{type:'text/csv'}));a.download='restaurant-payments.csv';a.click()};return <RestaurantSimpleWorkspace title="Finance" description="Actual settled and outstanding orders."><div className="grid gap-4 md:grid-cols-3"><Box label="Cash collected" value={money(cash)}/><Box label="Online collected" value={money(online)}/><Box label="Unpaid orders" value={String(records.length-paid.length)}/></div><button onClick={exportCsv} className="mt-5 rounded-xl border border-border px-4 py-2 text-sm"><Download className="mr-1 inline size-4"/>Export CSV</button></RestaurantSimpleWorkspace>}
export function RestaurantAnalytics(){const state=useRestaurant(async t=>{const{data,error}=await s().from('restaurant_orders').select('total,status,created_at').eq('tenant_id',t);if(error)throw error;return data??[]});if(!state.data)return <RestaurantSimpleWorkspace title="Analytics" description={state.error||'Loading live analytics…'}/>;const completed=state.data.filter((o:any)=>o.status!=='cancelled');const total=completed.reduce((n:number,o:any)=>n+Number(o.total),0);return <RestaurantSimpleWorkspace title="Analytics" description="Live performance calculated from your orders."><div className="grid gap-4 sm:grid-cols-3"><Box label="Orders" value={String(completed.length)}/><Box label="Revenue" value={money(total)}/><Box label="Average order" value={money(completed.length?total/completed.length:0)}/></div></RestaurantSimpleWorkspace>}
export function RestaurantMarketing(){const state=useRestaurant(async t=>{const{data,error}=await s().from('restaurant_promotions').select('*').eq('tenant_id',t).order('created_at',{ascending:false});if(error)throw error;return data??[]});const[name,setName]=useState('');const[discount,setDiscount]=useState('');const add=async()=>{if(!name)return;const{error}=await s().from('restaurant_promotions').insert({tenant_id:state.tenant,name,discount_percent:Number(discount||0)});if(error)alert(error.message);else{setName('');setDiscount('');state.reload()}};if(!state.data)return <RestaurantSimpleWorkspace title="Marketing" description={state.error||'Loading promotions…'}/>;return <RestaurantSimpleWorkspace title="Marketing" description="Create promotions for your own operational campaigns."><div className="flex gap-2"><input value={name} onChange={e=>setName(e.target.value)} placeholder="Offer name" className="flex-1 rounded-xl border border-border p-2"/><input value={discount} onChange={e=>setDiscount(e.target.value)} placeholder="Discount %" type="number" className="w-28 rounded-xl border border-border p-2"/><button onClick={add} className="rounded-xl bg-primary px-3 text-primary-foreground"><Plus/></button></div>{state.data.map((p:any)=><div key={p.id} className="mt-3 flex justify-between rounded-xl border border-border p-4"><b>{p.name}</b><span>{p.discount_percent}% · {p.active?'Active':'Disabled'}</span></div>)}</RestaurantSimpleWorkspace>}
export function RestaurantStaff(){const state=useRestaurant(async t=>{const{data,error}=await s().from('profiles').select('id,full_name,phone,role').eq('tenant_id',t);if(error)throw error;return data??[]});if(!state.data)return <RestaurantSimpleWorkspace title="Staff" description={state.error||'Loading staff…'}/>;return <RestaurantSimpleWorkspace title="Staff" description="Staff accounts are managed securely through RVC account registration."><div className="space-y-2">{state.data.map((p:any)=><div key={p.id} className="flex justify-between rounded-xl border border-border p-4"><span><b>{p.full_name||'Unnamed staff'}</b><small className="ml-2 text-muted-foreground">{p.phone||'—'}</small></span><span className="capitalize">{p.role.replace('_',' ')}</span></div>)}</div></RestaurantSimpleWorkspace>}
export function RestaurantSupport(){const state=useRestaurant(async t=>{const{data,error}=await s().from('support_tickets').select('*').eq('tenant_id',t).order('created_at',{ascending:false});if(error)throw error;return data??[]});const[subject,setSubject]=useState('');const send=async()=>{if(!subject)return;const user=(await s().auth.getUser()).data.user;const{error}=await s().from('support_tickets').insert({tenant_id:state.tenant,subject,created_by:user?.id});if(error)alert(error.message);else{setSubject('');state.reload()}};if(!state.data)return <RestaurantSimpleWorkspace title="Support" description={state.error||'Loading support…'}/>;return <RestaurantSimpleWorkspace title="Support" description="Send a real ticket directly to RVC Control."><div className="flex gap-2"><input value={subject} onChange={e=>setSubject(e.target.value)} placeholder="How can we help?" className="flex-1 rounded-xl border border-border p-2"/><button onClick={send} className="rounded-xl bg-primary px-4 text-primary-foreground">Send</button></div>{state.data.map((x:any)=><div key={x.id} className="mt-3 rounded-xl border border-border p-4"><b>{x.subject}</b><span className="float-right capitalize text-primary">{x.status}</span></div>)}</RestaurantSimpleWorkspace>}
export function RestaurantSettings(){const state=useRestaurant(async t=>{const{data,error}=await s().from('restaurant_settings').select('*').eq('tenant_id',t).single();if(error)throw error;return data as any});const[form,setForm]=useState<any>(null);const[notice,setNotice]=useState('');const[saving,setSaving]=useState(false);useEffect(()=>{if(state.data)setForm(state.data)},[state.data]);const save=async()=>{if(!state.tenant){setNotice('Restaurant profile is still loading. Please wait and try again.');return}setSaving(true);setNotice('');const{error}=await s().from('restaurant_settings').update({display_name:form.display_name,address:form.address,phone:form.phone,ordering_enabled:form.ordering_enabled,order_notifications:form.order_notifications,updated_at:new Date().toISOString()}).eq('tenant_id',state.tenant);setSaving(false);if(error)setNotice(`Save failed: ${error.message}`);else{setNotice('Settings saved successfully.');state.reload()}};if(!form)return <RestaurantSimpleWorkspace title="Settings" description={state.error||'Loading restaurant settings…'}/>;return <RestaurantSimpleWorkspace title="Settings" description="Your saved settings control your public QR ordering experience."><div className="max-w-xl space-y-3">{notice&&<p className={`rounded-xl p-3 text-sm ${notice.startsWith('Settings')?'bg-emerald-500/10 text-emerald-700':'bg-red-500/10 text-red-600'}`}>{notice}</p>}{[['display_name','Restaurant name'],['address','Address'],['phone','Phone']].map(([k,l])=><label key={k} className="block text-sm">{l}<input value={form[k]||''} onChange={e=>setForm({...form,[k]:e.target.value})} className="mt-1 w-full rounded-xl border border-border p-2"/></label>)}<label className="flex gap-2"><input type="checkbox" checked={form.ordering_enabled} onChange={e=>setForm({...form,ordering_enabled:e.target.checked})}/>Accept QR orders</label><button disabled={saving} onClick={save} className="rounded-xl bg-primary px-4 py-2 text-primary-foreground disabled:opacity-50"><Save className="mr-1 inline size-4"/>{saving?'Saving…':'Save settings'}</button></div></RestaurantSimpleWorkspace>}
export function RestaurantBilling(){return <RestaurantSimpleWorkspace title="RVC billing" description="Subscription changes are managed by RVC Control. Your current plan and invoices appear here once issued."/>};function Box({label,value}:{label:string;value:string}){return <div className="rounded-xl border border-border bg-card p-4"><p className="text-sm text-muted-foreground">{label}</p><p className="mt-1 text-xl font-semibold">{value}</p></div>}
