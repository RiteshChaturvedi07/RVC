'use client'
export type ReceiptOrder={order_number:number;created_at?:string;payment_status:string;total:number;items?:{name:string;quantity:number;unit_price?:number;line_total?:number}[]}
export type ReceiptData={restaurant:string;address?:string;phone?:string;gstin?:string;table:string;orders:ReceiptOrder[];discount?:number;tax?:number}
export function printReceipt(data:ReceiptData){const items=data.orders.flatMap(order=>order.items??[]);const subtotal=items.reduce((n,item)=>n+Number(item.line_total??(item.unit_price??0)*item.quantity),0);const total=data.orders.reduce((n,order)=>n+Number(order.total),0);const paid=data.orders.every(order=>order.payment_status==='paid');const rows=items.map(item=>`<tr><td>${item.name}</td><td>${item.quantity}</td><td>₹${Number(item.unit_price??0).toFixed(2)}</td><td>₹${Number(item.line_total??(item.unit_price??0)*item.quantity).toFixed(2)}</td></tr>`).join('');const win=window.open('','_blank','width=420,height=720');if(!win)return;win.document.write(`<!doctype html><html><head><title>Receipt</title><style>@page{margin:5mm}*{box-sizing:border-box}body{font-family:Arial,sans-serif;color:#111;margin:0 auto;width:80mm;font-size:12px}.center{text-align:center}h2{margin:0 0 5px;font-size:18px}hr{border:0;border-top:1px dashed #555;margin:10px 0}table{width:100%;border-collapse:collapse}td,th{padding:4px 0;text-align:left;font-size:11px}td:nth-child(n+2),th:nth-child(n+2){text-align:right}.total{font-size:16px;font-weight:700}.badge{display:inline-block;padding:4px 8px;border:1px solid #111;font-weight:700}@media print{body{width:80mm}}@page{size:auto}</style></head><body><div class="center"><h2>${data.restaurant}</h2><div>${data.address||''}</div><div>${data.phone||''}</div>${data.gstin?`<div>GSTIN: ${data.gstin}</div>`:''}<hr/><b>Table ${data.table}</b><br/>${new Date().toLocaleString('en-IN')}<br/>Orders: ${data.orders.map(x=>`#${x.order_number}`).join(', ')}<hr/><span class="badge">${paid?'PAID':'UNPAID'}</span></div><table><thead><tr><th>Item</th><th>Qty</th><th>Rate</th><th>Amount</th></tr></thead><tbody>${rows}</tbody></table><hr/><table><tr><td>Subtotal</td><td>₹${subtotal.toFixed(2)}</td></tr>${data.discount?`<tr><td>Discount</td><td>-₹${data.discount.toFixed(2)}</td></tr>`:''}${data.tax?`<tr><td>Tax</td><td>₹${data.tax.toFixed(2)}</td></tr>`:''}<tr class="total"><td>Grand Total</td><td>₹${total.toFixed(2)}</td></tr></table><hr/><div class="center">Thank you for dining with us!<br/><small>Powered by RVC</small></div><script>window.onload=()=>window.print()</script></body></html>`);win.document.close()}
